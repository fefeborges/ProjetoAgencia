@model IEnumerable<ProjetoAgencia.Models.Estadia>

@{
    ViewData["Title"] = "Index";
}

<h1>Estadia</h1>

<p>
    <a class="btncriar" asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeEstadia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEstadia)
            </td>
            <td>
                <a class="a btnEditar" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a class="a btnDetalhes" asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                <a class="a btnDeletar" asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
